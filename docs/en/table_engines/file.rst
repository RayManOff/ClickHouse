File(InputFormat)
-----------------

The data source is a file that stores data in one of the supported input formats (TabSeparated, Native, и т. д.) ...

For a table with a source File, ClickHouse reads the file again, with each SELECT.
